<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Your Results</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
</head>
<body>
    <div class="container text-center">
        <h1 class="display-4 text-center">The Results!</h1>
        <p>Let's see how you did, <%= answers.fname %>!</p>
        <p>
            <%= answers.meaning %>
            <% if (answers.meaning == "pufferfish" || answers.meaning == "puffer fish" || answers.meaning == "blowfish" || answers.meaning == "blow fish"){ %>
                <span class="badge badge-success">Right!</span>
            <% }else{ %>
                <span class="badge badge-danger">Wrong!</span>
            <% } %>
        </p>
        <p>
            <%= answers.toxin %>
            <% if (answers.toxin.toLowerCase() == "tetrodotoxin"){ %>
                <span class="badge badge-success">Right!</span>
            <% }else{ %>
                <span class="badge badge-danger">Wrong!</span>
            <% } %>
        </p>
        <p>
            <%= answers.deaths %>
            <% if (answers.deaths == 7){ %>
                <span class="badge badge-success">Right!</span>
            <% }else{ %>
                <span class="badge badge-danger">Wrong!</span>
            <% } %>
        </p>
        <p>
            <%= answers.country %>
            <% if(answers.country.toLowerCase() == "haiti"){ %>
                <span class="badge badge-success">Right!</span>
            <% }else{ %>
                <span class="badge badge-danger">Wrong!</span>
            <% } %>
        </p>
        <div class="mx-auto" style="width: 200px;">
            <a name="back" href="/" class="btn btn-secondary text-white">Go Back!</a>
        </div>
    </div>
</body>
</html>